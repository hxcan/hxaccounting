<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- POST_NOTIFICATIONS：Android 13+ 发送通知需要此权限（upgrademanager 库使用） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ACCESS_COARSE_LOCATION：appstore-core 库需要同时声明 FINE + COARSE（Android 12+ 强制），
         见 #861808091040 后续优化移除 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".HxAccountingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HxAccounting">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.QuickAddActivity"
            android:exported="false"
            android:label="@string/title_quick_add"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity
            android:name=".ui.AccountsActivity"
            android:exported="false"
            android:label="@string/title_accounts"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.BudgetSettingsActivity"
            android:exported="false"
            android:label="@string/title_budget_settings"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>